- name: Manage Batocera VGA baseline
  hosts: batocera_hosts
  gather_facts: false
  vars:
    config: "{{ lookup('file', playbook_dir ~ '/../../config/homelab.yaml') | from_yaml }}"
  roles:
    - role: batocera_baseline
      tags:
        - batocera_baseline
    - role: batocera_vga_safe
      tags:
        - batocera_vga_safe
