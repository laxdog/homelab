- name: Configure guests
  hosts: guests
  become: true
  gather_facts: true
  vars_files:
    - ../secrets.yml
  vars:
    config: "{{ lookup('file', playbook_dir ~ '/../../config/homelab.yaml') | from_yaml }}"
  tasks:
    - name: Placeholder for guest roles
      ansible.builtin.debug:
        msg: "Guest configuration not implemented yet"
