---
- name: Install base utilities on host
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - git
      - jq
      - tmux
      - vim
    state: present
    update_cache: true
