---
- name: Install base utilities on host
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - git
      - jq
      - nmap
      - tmux
      - vim
    state: present
    update_cache: true
