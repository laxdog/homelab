# Backups

## Proxmox
- vzdump targets `/tank/backups`

## ZFS replication
- replicate `/tank/personal` and `/tank/backups` to offsite (TrueNAS)
- bulk media replication is optional and off by default
